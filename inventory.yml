all:
  hosts:
    # Example hosts (using local connection for testing)
    web_server_01:
      ansible_host: 127.0.0.1
      ansible_connection: local
      # ansible_user: my_user
      # ansible_ssh_private_key_file: ~/.ssh/id_rsa_web
    db_server_01:
      ansible_host: 127.0.0.1
      ansible_connection: local
  children:
    standard_servers:
      # vars:
      #   ansible_user: admin
      #   ansible_ssh_private_key_file: ~/.ssh/id_rsa_web
      hosts:
        web_server_01:
    custom_servers:
      hosts:
        db_server_01:
